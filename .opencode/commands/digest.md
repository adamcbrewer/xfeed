---
description: Scrape X timeline and generate a personalised digest
---

Use the xfeed skill to generate a daily digest from the following scraped feed data.

## User Interests
Refer to the project's `config.yaml` (or `config.yml`) for the user's `interests` and `priorityAccounts` lists.

## Feed Data
!`pnpm digest:scrape-only 2>/dev/null`
